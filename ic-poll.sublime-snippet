<snippet>
  <content><![CDATA[
setTimeout(function poll() {
  //folgende Abfrage evtl. entfernen bzw austauschen durch Bedingung
  if ((typeof s !== "undefined") && (typeof s === "object") && (typeof s.tl === "function")) {
        //do sth
  } else {
    poll();
  }
}, 100); //wait 100 ms
]]></content>
  <tabTrigger>ic-Poll</tabTrigger>
  <description>Poll until condition is met</description>
</snippet>
